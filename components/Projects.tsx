"use client";
import{Code2,MoveUpRight}from"lucide-react";import{MouseEvent}from"react";import{projects}from"@/data/portfolio";import{Section}from"./Section";import{PipelineScene}from"./ThreeScenes";
export default function Projects(){const tilt=(e:MouseEvent<HTMLElement>)=>{if(window.matchMedia("(pointer:coarse)").matches)return;const el=e.currentTarget,r=el.getBoundingClientRect();el.style.setProperty("--rx",`${((e.clientY-r.top)/r.height-.5)*-7}deg`);el.style.setProperty("--ry",`${((e.clientX-r.left)/r.width-.5)*7}deg`)};return <Section id="projects" eyebrow="04 / Selected work" title="Pipelines with measurable impact." intro="Tiga studi kasus placeholder—ganti metrik dan link dengan proyek asli Anda."><PipelineScene/><div className="project-grid">{projects.map(p=><article className="project-card" key={p.title} onMouseMove={tilt} onMouseLeave={e=>{e.currentTarget.style.setProperty("--rx","0deg");e.currentTarget.style.setProperty("--ry","0deg")}}><div className="project-top"><span>{p.number}</span><div><a aria-label={`GitHub ${p.title}`} href="https://github.com/your-username" target="_blank" rel="noreferrer"><Code2 size={19}/></a><a aria-label={`Demo ${p.title}`} href="#contact"><MoveUpRight size={19}/></a></div></div><p className="project-subtitle">{p.subtitle}</p><h3>{p.title}</h3><p>{p.description}</p><div className="mini-flow">{p.flow.map((f,i)=><span key={f}>{f}{i<p.flow.length-1&&<i>→</i>}</span>)}</div><strong className="impact">{p.impact}</strong><div className="badges">{p.stack.map(s=><span key={s}>{s}</span>)}</div></article>)}</div></Section>}
