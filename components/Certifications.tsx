import{Award,BadgeCheck}from"lucide-react";import{certifications}from"@/data/portfolio";import{Section}from"./Section";
export default function Certifications(){return <Section id="certifications" eyebrow="05 / Credentials" title="Always learning, deliberately."><div className="cert-grid">{certifications.map(c=><article className="cert-card" key={c.title}><div className="cert-seal"><Award size={26}/><span>{c.short}</span></div><div><p>{c.issuer} · {c.year}</p><h3>{c.title}</h3></div><BadgeCheck className="verified" size={22}/></article>)}</div><p className="placeholder-note">Sertifikasi di atas adalah placeholder. Tampilkan hanya sertifikasi yang benar-benar Anda miliki.</p></Section>}
