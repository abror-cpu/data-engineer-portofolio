"use client";
import{useEffect,useState}from"react";import{Menu,Moon,Sun,X}from"lucide-react";import gsap from"gsap";import{ScrollTrigger}from"gsap/ScrollTrigger";import Hero from"./Hero";import About from"./About";import Skills from"./Skills";import Experience from"./Experience";import Projects from"./Projects";import Certifications from"./Certifications";import Contact from"./Contact";
const links=[["About","about"],["Skills","skills"],["Experience","experience"],["Projects","projects"],["Contact","contact"]];
export default function Portfolio(){const[dark,setDark]=useState(true),[menu,setMenu]=useState(false),[loading,setLoading]=useState(true);useEffect(()=>{document.documentElement.dataset.theme=dark?"dark":"light";localStorage.setItem("theme",dark?"dark":"light")},[dark]);useEffect(()=>{const saved=localStorage.getItem("theme");if(saved)setDark(saved==="dark");const timer=setTimeout(()=>setLoading(false),950);gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>{gsap.utils.toArray<HTMLElement>(".reveal-section").forEach(el=>gsap.fromTo(el,{opacity:0,y:48},{opacity:1,y:0,duration:.85,ease:"power3.out",scrollTrigger:{trigger:el,start:"top 82%",once:true}}))});return()=>{clearTimeout(timer);ctx.revert()}},[]);useEffect(()=>{const cursor=document.querySelector<HTMLElement>(".custom-cursor");if(!cursor||window.matchMedia("(pointer:coarse)").matches)return;const move=(e:globalThis.MouseEvent)=>{cursor.style.transform=`translate3d(${e.clientX}px,${e.clientY}px,0)`};const over=(e:globalThis.MouseEvent)=>cursor.classList.toggle("active",!!(e.target as HTMLElement).closest("a,button,input,textarea"));window.addEventListener("mousemove",move);window.addEventListener("mouseover",over);return()=>{window.removeEventListener("mousemove",move);window.removeEventListener("mouseover",over)}},[]);return <><div className={`loader ${loading?"":"loaded"}`}><div className="loader-mark"><span>HA</span><i/></div><p>Initializing data systems</p></div><div className="custom-cursor" aria-hidden="true"/><header className="nav-shell"><a className="logo" href="#home" aria-label="Beranda"><span>H</span>A</a><nav className={menu?"open":""}>{links.map(([label,id])=><a key={id} href={`#${id}`} onClick={()=>setMenu(false)}>{label}</a>)}</nav><div className="nav-actions"><button className="icon-btn" onClick={()=>setDark(!dark)} aria-label="Ganti tema">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><a className="nav-cta" href="#contact">Let&apos;s talk <span>↗</span></a><button className="menu-btn" onClick={()=>setMenu(!menu)} aria-label="Buka menu">{menu?<X/>:<Menu/>}</button></div></header><main><Hero/><About/><Skills/><Experience/><Projects/><Certifications/><Contact/></main><footer><a className="logo" href="#home"><span>H</span>A</a><p>Designed around data. Built for impact.</p><p>© 2026 Hikmal Abror</p></footer></>}
